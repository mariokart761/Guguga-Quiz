## 安裝與使用
```
pip install beautifulsoup4
python exam_to_json_parser.py "your_file.html"
python exam_to_json_parser.py "quiz_html" -o "output"
```

## 其他格式轉換

- 可提供你的試卷檔案給大模型，生成 python 腳本，批量轉換為範例格式(`output_example.json`)即可。